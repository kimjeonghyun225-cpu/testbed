# -----------------------------------------------------------------------------
# devices_auto 환경변수 템플릿 (PowerShell)
# - 이 파일은 "예시/템플릿"입니다. 실제 토큰/키는 절대 커밋하지 마세요.
# - 로컬 실행 권장:
#   1) 이 파일을 참고해서 Windows 사용자 환경변수로 등록하거나
#   2) `devices_auto/.env` 파일을 만들어 KEY=VALUE 형태로 저장(이 파일은 gitignore)
# -----------------------------------------------------------------------------

$env:JIRA_BASE_URL = "https://YOUR_DOMAIN.atlassian.net"
$env:JIRA_EMAIL    = "YOUR_EMAIL@example.com"
$env:JIRA_API_TOKEN = "YOUR_JIRA_API_TOKEN"
$env:JIRA_AUTH_TYPE = "atlassian"
$env:JIRA_API_VERSION = "3"
$env:JIRA_JQL = 'project in (PGPAM, PSAB) AND issuetype = Bug AND created >= -180d ORDER BY created DESC'


# Confluence Cloud (Basic 추천)
$env:CONF_BASE_URL="https://YOUR_DOMAIN.atlassian.net/wiki"
$env:CONF_EMAIL="YOUR_EMAIL@example.com"
$env:CONF_TOKEN="YOUR_CONFLUENCE_TOKEN"

# LLM
$env:OPENAI_API_KEY="YOUR_OPENAI_API_KEY"
$env:OPENAI_MODEL="gpt-5.2"